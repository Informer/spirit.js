(function(ns) {

  'use strict';

  /**
   * Construct a GSAP timeline
   * @param transitions spirit.collection.TransitionCollection
   * @returns {Timeline(Lite/Max)}
   */
  ns.Timeline = function Timeline(transitions){

  };

})(use('spirit.factory'));
